version: '3.8'

# Docker Compose для разработки с hot-reload
services:
  frontend-dev:
    image: node:20-alpine
    container_name: social-housing-frontend-dev
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
    command: sh -c "npm install && npm run dev"
    restart: unless-stopped
    networks:
      - social-housing-network

networks:
  social-housing-network:
    driver: bridge
